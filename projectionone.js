const {client}=require("./dbconfig");

const projectionOne=async()=>{

    try {

        const database = client.db("school");

        const students = database.collection("students");

        const query ={};

        itemProjetion={}
        
    } catch (error) {
        
    }
}